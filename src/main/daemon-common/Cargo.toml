[package]
name = "poratake-daemon-common"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-only"
rust-version.workspace = true

[features]
qrcode-decode = ["dep:rqrr"]

[dependencies]
rqrr = { version = "0.10.1", default-features = false, optional = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
